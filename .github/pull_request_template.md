## Change

Describe the user-visible problem and resulting behavior.

## Verification

- [ ] `./scripts/verify-project.ps1 -BuildRoot build-check -Config Release`
- [ ] Relevant host/state compatibility checks and UI captures

Record any unrun checks or known limitations. Parameter IDs and existing project recall must remain compatible; document deliberate state migrations.
