# UI Priority Audit

This audit tracks layout and control-placement work that most directly improves Chipper as a usable, chip-aware instrument.

## Current Audit Focus

The current UI baseline is source-card ownership plus readable standard controls. New screenshot feedback should be turned into durable rules here, then pruned from the active queue once the rule is implemented and verified. Fixed audio bugs such as FM held-tail and NES DMC loop-off only belong in this audit when a UI change could reintroduce them. Broad product backlog items belong in `product-gap-roadmap.md`; this file is the visual and control-placement checklist.

Review status: synced on 2026-07-27 after a fresh 164-image audit: all 27 implemented modes in editor, preset browser, and Motion Lab at 1180/1240 px (162 captures), plus the OPL3 four-operator workspace at both widths (2 captures). The matrix showed no global clipping, overlap, missing-workspace, or width-collapse regression. It found one contained defect—the compact OPL waveform box painted only its frame—which is now fixed and protected by a pixel-level editor smoke assertion. Active UI work should now follow engine-backed FM/sample slices or a reproduced visual regression, not another broad layout pass.

Command-line release gates live in [release-builds.md](release-builds.md). This audit should keep only the visual rules that prevent users from misunderstanding or losing access to chip behavior. When an audio bug is fixed and tested, keep the UI implication here only if it affects what users can see, choose, or trust.

## Planning Hygiene

This file should stay visual. If a bug is fixed by a renderer or processor gate, keep only the UI rule that prevents confusing the user. Do not keep old audio bugs in the active UI queue unless a UI change can reintroduce them or the current screenshot shows the same class of visual failure.

For every new screenshot issue, reduce it to one of these reusable questions before editing:

- Is the control owned by the channel, lane, voice, operator, or shared chip block that actually controls the sound?
- Is the control standard height and readable at the default editor size?
- Does the visible wording tell the truth about native hardware versus Chipper helper behavior?
- Does the panel have enough room for all controls without hiding level, sample, wave, loop, or envelope state?
- If the issue is fixed, is there a descriptor, renderer, screenshot, or checklist gate that makes it less likely to regress?

## Current Visual Gate

Before a UI slice is considered done, inspect the changed chip at the default editor size and one neighboring chip from a different layout family. The pass should confirm:

- no hidden, undersized, or detached source-level lanes
- no shrunken dropdowns or numeric boxes where standard-height controls should fit
- visible dropdowns remain at least 48x24 px, visible buttons at least 24x18 px, and visible sliders keep a readable rail in `chipper_editor_size_smoke`
- no overlap between sample-bank editors, waveform previews, and Performance Macros
- source-owned controls stay in the source cards
- footer verification wording remains visible and truthful
- the Performance Macros strip never draws over chip-owned editors such as sample banks, waveform previews, operator grids, or source cards

## Non-Regression Checklist

- overlay playheads and active states use a non-color cue as well as contrast; Motion Lab uses `STEP N >` plus an accent border/fill
- preset-browser and Motion Lab overlays replace one another deterministically, remain above the unified editor, and keep Escape/focus-return behavior predictable
- Do not number sections. Hidden or chip-specific modules make numbering misleading.
- Keep the header focused on preset, chip mode, Strictness, and Play Mode. Verification strength belongs in the footer.
- Use standard-height dropdowns, buttons, and numeric inputs. The smoke-test floor is 48x24 px for visible ComboBoxes and 24x18 px for visible TextButtons; if a control cannot fit at that size, grow the card or layout.
- Never let labels, readouts, dropdowns, sliders, or waveform views overlap. Readability beats compactness.
- A waveform, algorithm, or signal preview must show meaningful content, not merely a bordered empty box. At compact heights, remove a redundant internal label before removing the trace or diagram.
- Channel-local register controls live in their channel card: duty under pulse channels, sample/wave selectors under sample/wavetable voices, and noise controls under noise channels.
- Shared modules are only for shared hardware or shared workflow: filters, echo, sample banks, output routing, global clocks, and cross-channel pairing.
- Sample and wavetable chips must reserve a real waveform/sample workspace. Performance macros may be compact, but they must not overlap sample-bank controls.
- When a chip has per-voice or per-lane ownership, the source card should show the playable path in one place: waveform or sample selector, key/loop/noise mode where applicable, level, and any visible helper envelope state. If those controls cannot fit, the layout grows instead of hiding them. The Level lane should be immediately readable after the source selector rather than squeezed into a thin strip or stranded at the bottom of a tall card.
- Footer verification language must remain truthful: **Verified partial** is acceptable; **Cycle accurate** is not allowed without accepted tests or hardware/reference proof.
- Chip themes may differ, but contrast, focus, hit targets, and readable text size must stay consistent.
- Logo underline decorations should align to the full logo width so the header feels intentional on every theme.
- Screenshot feedback should become durable layout rules in this audit. Avoid preserving one-off visual bug notes after the underlying rule is captured and verified.
- Fixed audio regressions should only appear here when they constrain UI changes. The active UI queue should focus on visible chip-aware controls, readable source cards, sample/wave/operator editing, and screenshot regressions that still reproduce.
- If a visual problem has already been fixed, keep only the reusable rule that would prevent it from returning. If a chip still violates that rule in screenshots, track the chip-specific fix under Highest-Value Next Fixes.
- If a screenshot issue is fixed in code, prefer adding or tightening a descriptor/render/checklist gate over preserving the screenshot as a permanent todo.
- If a non-visual audio bug is fixed, keep the UI implication here only when it affects what users can see or choose. The actual audio memory belongs in the regression gate table, not in every visual backlog.

## Completed Pattern

- Game Boy / DMG now treats channel cards as the owner of channel-native register controls: Pulse 1 owns duty plus NR10 Sweep Shift, Pulse 2 owns independent NR21 duty, Wave owns its 32-sample RAM shape plus NR32 level, and Noise owns both LFSR width and NR43 Noise Clock.
- DMG's current APVTS model exposes one shared pulse initial-level helper and one shared 64 Hz envelope-decay helper for Pulse 1, Pulse 2, and Noise. The UI keeps those together in `Pulse + Noise Envelopes` and records independent per-channel envelopes and zombie-envelope techniques as a product gap instead of duplicating one parameter into misleading channel controls.
- DMG Note Allocation is explicit: Chip Poly assigns notes to Pulse 1, Pulse 2, and Wave only; the Noise card remains visible as an SFX lane and says that it is not note-allocated.
- DMG uses a compact 720 px chip-specific editor height. The four complete channel paths, envelope helpers, NR51 routing, native clock override, and output remain visible at both supported widths; the smaller canvas removes blank card bodies without shrinking interaction controls.
- NES / RP2A03 should follow the same channel-ownership pattern: Pulse 1 duty lives under Pulse 1, Pulse 2 duty lives under Pulse 2, and Noise mode lives under Noise / DMC. The old Shape / Mixer panel should not be the primary place users edit these register-like choices.
- NES / RP2A03 now reserves more vertical room for those channel-owned controls and explicitly clears the old global Pulse 1 Duty slot in the Performance strip. If Pulse 2 appears to follow Pulse 1, verify whether Pulse 2 Duty is set to `Follow`; explicit 12.5/25/50/75 choices are independent `$4004` duty overrides.
- Game Boy / DMG follows the same rule: Pulse 2's `Follow` state intentionally follows or offsets Pulse 1 according to the selected recipe/play mode, while explicit 12.5/25/50/75 choices write the `NR21` duty bits independently.
- SN76489 / Sega PSG follows the same ownership rule for noise: the noise-control register choice belongs under the Noise source card, not in a detached Tone / Crunch panel.
- SN76489 now carries that rule through the entire native noise path: the taller Noise card owns Noise Mode, preset-only Noise Bias, native 4-bit attenuation, and the modern post-attenuation Level trim. Explicit Noise Mode choices visibly disable the preset-only bias control.
- SN76489's three square-tone cards stay compact, with Tone Stack and Pitch Motion grouped immediately below them. This preserves the important Tone 3/noise-clock relationship without pretending the tone lanes need the same control depth as Noise.
- SN76489 Chip Poly allocates Tone 1–3 only. Noise remains visible as an SFX lane and explicitly says it is not note-allocated. The complete surface uses the smoke-tested 720 px compact height at both supported widths.
- Philips SAA1099 now presents all six stereo output lanes in two visible rows matching its shared-generator groups: channels 1-3 use Noise 0/Envelope 0 and channels 4-6 use Noise 1/Envelope 1. Each channel card reports its resolved left/right amplitude nibbles, while Noise Mode/Clock and Envelope Shape/Speed stay together in one Shared Generators block. This avoids inventing extra noise lanes or scattering the two group relationships across destinations.
- SAA1099's six tone lanes remain the six Chip Poly allocation targets. Noise masks are shared group state, not seventh and eighth note channels. The 780 px surface and both supported widths are protected by structural ownership, row-grouping, control-size, and overlap checks.
- Atari POKEY now uses a 2x2 channel matrix: horizontal rows preserve the 1+2 and 3+4 16-bit pairings, while vertical columns preserve the 3-to-1 and 4-to-2 high-pass relationships. AUDCTL Pairing and Filter are adjacent in a dedicated relationship block instead of being split between Tone and Output destinations.
- POKEY pairing is reflected in the playable surface. A paired high-byte channel is labelled as consumed, its inactive enable/trim controls are disabled without losing their parameter state, and Chip Poly renumbers the remaining three or two note lanes. Distortion Code, preset-only Distortion Bias, and the modern AUDV Gate remain one shared block because the current partial engine resolves one AUDC texture/AUDV base across sounding channels.
- POKEY channel sliders are modern post-AUDV trims, not independent AUDV registers. Cards say `Trim`, show the shared AUDV nibble plus trim percentage, and keep exact AUDF/AUDC/AUDCTL detail in tooltips/readouts. Both supported widths, relationship ownership, pairing state, bias enablement, and note-allocation labels are structural smoke-test contracts.
- PC Speaker now presents its complete audible architecture as one compact mono path: PIT channel 2 -> port `0x61` -> speaker cone. The one source card owns enable and modern trim; Speaker Mode, Pulse Width, Pitch Motion, Click Grit, Speaker Level, and the Chipper Gate Decay helper stay together in the path block rather than being scattered across generic Tone, Envelope, and Performance destinations.
- PC Speaker intentionally has no mixer or Chip Poly surface. Only its clock override and final output are global. The 720 px surface, both supported widths, source ownership, named hardware path, control containment, and non-overlap are structural smoke-test contracts.
- ZX Spectrum Beeper now presents its complete audible architecture as one ULA path: generated CPU-style toggles -> port `$FE` -> EAR bit 4 / MIC bit 3 -> one-bit beeper. Beeper Mode and all five shaping controls stay in that path, while the source card owns enable and modern trim.
- ZX wording must distinguish the actual port state from Chipper's musical helpers. MIC names bit 3, the mode selector exposes EAR/MIC combinations, and `Duty + Border` explains that bits 2-0 are border metadata while duty is generated timing color rather than a native register. The 720 px surface, both supported widths, absent Chip Poly, named hardware path, control containment, and non-overlap are structural smoke-test contracts.
- Embedded channel controls must suppress their old standalone labels and readouts; otherwise stale labels can reappear after chip switching or parameter refresh.
- HuC6280 now has a dedicated three-column, two-row `Wave RAM + Noise Voices` deck. All six channels own their preview, wave choice, and Level trim; channels 5/6 explicitly advertise hardware noise, and the channel 2 to channel 1 LFO relationship lives immediately beneath the cards.
- HuC6280, Namco WSG, and SCC use a shared 880 px wavetable surface budget so the native Wave Lab remains inline between the source deck and shared output footer. Namco keeps eight equivalent 32 x 4-bit lanes in a 4x2 bank; SCC keeps five 32-byte 8-bit cards in a centered 3+2 bank plus its enhanced-core topology disclosure.
- Wavetable and sampler channel cards should stack controls in the same order users hear them: enable/header, waveform preview, per-voice wave or sample selector, then level. Dense channel cards now keep the level lane directly in the reading flow so wave/sample selectors cannot crowd out gain visibility or strand the slider at the bottom of a tall card.
- Amiga Paula now has a dedicated 900 px unified tracker surface. Four fixed L/R/R/L DMA channel cards each own enable/state, waveform evidence, generated Shape, loaded Sample slot, modern Level trim, resolved 0-64 volume, and loop/one-shot state.
- Paula channel cards use labeled inline Shape and Sample rows followed immediately by Level. Chip Poly identifies Note 1-4 in the four channel headers; Big Mono keeps all four stacked lanes visible and exposes Channel Spread.
- Channel-local wave/sample selectors must stay visible from the source-card surface, even when the old standalone Wave Shape segment is hidden. This prevents Paula, HuC6280, Namco WSG, SCC, and SPC700 from losing their per-voice controls during chip switches or visibility refreshes.
- Source cards are authoritative live controls, while presets are starting recipes. If a user explicitly enables a visible source card or chooses a channel-local register value, the engine must make that source audible when practical instead of silently preserving a preset mask or zero-volume recipe. This is now guarded for NES/RP2A03 and Game Boy/DMG mono paths so independent Pulse 2 duty and source enable controls cannot look active while producing no audio.
- HuC6280, Namco WSG, and SCC all use 136-144 px cards for different reasons: HuC headers communicate LFO/noise roles, Namco fits eight readable 4-bit lanes into a balanced 4x2 bank, and SCC keeps five independent enhanced-mode 8-bit waves legible while reserving a visible topology disclosure above them.
- Paula's shared Sample Bank remains a protected editor for WAV/AIFF/8SVX/MOD import, manual/key/tracker mapping, map root, missing-asset state, loop metadata, and waveform evidence.
- `Tracker Playback + Paula Output` exposes every implemented shared behavior in two readable rows: Channel Spread, Period Motion, Loop Tendency, explicit Loop Mode, 0-64 Channel Volume, Tracker Amp Env, Output Filter, Stereo Spread, and Output.
- Paula stereo language is hardware-honest: full Stereo Spread is the authentic L/R/R/L hard-pan layout; zero is a modern centered mono collapse. Both supported widths, channel/sample ownership, Chip Poly labels, sample-bank geometry, shared-control containment, and this readout truth are structural smoke-test contracts.
- SNES SPC700-style now has a dedicated 900 px unified sampler surface. `Generated Source` separates the fallback generated shape from shared NON/noise playback, while `Voice Shaping` keeps Envelope Shape, Pitch/PMON, and ADSR/GAIN Speed adjacent instead of scattering them into generic Sample, Pitch, and Envelope destinations.
- All eight SPC700-style sample voices remain visible in a contained four-by-two matrix. Every card owns enable/state, waveform evidence, its loaded BRR/WAV/AIFF bank-slot pin, and a readable Level lane; the second row must never overrun the sample bank.
- The SPC700-style Sample Bank owns import, manual/note/drum mapping, map root, loop lifetime, waveform preview, and explicit Loop Start/End controls. The binary lifetime control says `Loop While Held` when enabled and `One Shot` when disabled; it remains distinct from the playback-mapping selector.
- Only genuinely shared SPC700-style behavior remains in `Voice Mix + Echo + Output`: Voice Spread, Echo Color, Voice Volume, modern Stereo Spread, and Output. The shared envelope/noise/PMON controls are labeled as templates/partial behavior rather than pretending the current engine exposes independent S-DSP registers per card.
- SPC700-style geometry is a two-width structural contract. At 1240 and the 1180 minimum, generated/noise selectors, voice-shaping controls, all eight cards, sample-bank controls, waveform/loop range, and the five-part shared strip must remain visible, owned, readable, and non-overlapping.
- NES / RP2A03 DMC sample controls now prioritize the sample bank and waveform preview over the APU envelope helper. APU decay moved into Performance Macros so the DMC panel can use a two-column sample-editor layout with a larger waveform surface.
- NES / RP2A03 DMC loop wording should distinguish true sample looping from the hardware DAC hold. The `Loop Sample` checkbox maps to the `$4010` loop bit; when it is off, status text should read like `One-shot, no loop; DAC holds` because DPCM bit stepping stops at the final bit while the DMC DAC holds its terminal level until the next trigger. `processor_midi_cc_smoke` is the quick regression check for manual-slot and note-map one-shot behavior, held mapped notes that must not retrigger after one-shot completion, loop-on behavior, debug playback state, and waveform-preview loop markers.
- FM modes should not be allowed to regress into sustained-note fade-out. Renderer `tailRms` held-tail assertions are in place for YM2612/OPN2, OPL2/OPL3, YM2151/OPM, YM2413/OPLL, and key factory presets; any FM UI/control pass touching operator envelopes, source levels, or key-on handling should keep that CTest subset green.
- Because FM held-tail behavior is currently fixed, FM planning should focus on user-visible operator editing, algorithm clarity, and envelope terminology. Do not keep "fix FM fade" as an active UI item unless a current build reproduces it and the held-tail gate fails.
- SID already follows this pattern for per-voice waveform and pulse-width controls, with the global filter staying in the Filter panel. SID ADSR is also a primary instrument surface: the three per-voice Attack/Decay/Sustain/Release lanes and their previews must keep a protected full-width module height before the Performance Macros strip is laid out.
- SID Performance Macros use a shorter single-row strip under the protected ADSR module. The ADSR module owns the extra vertical budget, while macros stay secondary and must not collide with or compress per-voice envelope controls.
- SID ADSR readability is now part of chipper_editor_size_smoke: the per-voice Attack/Decay/Sustain/Release sliders and envelope previews must remain visible, contained in the ADSR module, and above the Performance Macros strip at default and minimum widths.
- Shared helper controls must use neutral base wording and chip-specific resolved readouts. For example, a volume-gate helper on POKEY or Paula should never describe itself as a NES/APU envelope period.
- Visible preset-following states should say "Preset" rather than "Follow" when the control is using the selected preset recipe. "Shared Bank" is used for sample voices that inherit the global sample slot or note map.
- Envelope sections should use truthful chip taxonomy: **ADSR** for SID-style per-voice ADSR and S-DSP ADSR/GAIN work, **Operator EG** for FM operator envelopes, **APU/Volume/Hardware Envelope** for native non-ADSR chips, and **Amp Env/Volume Helper** when Chipper adds a musical helper over volume, attenuation, or sample playback.
- Chipper helper envelopes must name the underlying chip path when practical and must be honest about scope. POKEY says **AUDV Gate**, Paula says **Tracker Amp Env**, and HuC6280, Namco WSG, and SCC say **Chipper Gate** inside their unified shared footer. Avoid generic ADSR/Decay wording unless the chip really exposes that behavior.
- Helper-envelope parameter help is part of the UI contract too: non-native helper chips must not reuse NES copy or native ADSR wording, and their help should disclose the helper/gate layer so users understand what is authentic chip state versus a Chipper playability layer.
- Helper envelope modules must not be empty. If the engine only has a shared helper, show one standard-height shared control with an honest readout. If a future pass adds per-channel helper envelopes, move them into the channel cards so users can see which lane is being shaped.
- The code now exposes this as `EnvelopeModel` metadata via `envelopeModelFor()`, with descriptor-smoke coverage for native ADSR/GAIN, native Operator EG, native non-ADSR envelopes, Chipper helper envelopes, and the visible module titles for SPC700, FM, POKEY, Paula, HuC6280, Namco WSG, and SCC. Host/MIDI wording should use neutral names like **Envelope / EG** when one shared parameter spans multiple chips, while visible chip panels should use the chip-specific labels above.
- HuC6280, Namco WSG, and SCC no longer spend detached modules on helper envelopes: `Chipper Gate`, `Modern Width`, and Output occupy the second footer row.
- Embedded combo boxes and numeric inputs should use the standard control height wherever there is room. If a card cannot fit a standard-height dropdown plus label, increase the card or layout size instead of shrinking the dropdown into unreadable text. Dense wavetable and sampler source cards now reserve standard-height selectors and a clearer level lane.
- Grouped slider readouts must not depend on leftover slivers of space. If a panel is compact, place the readout in the header row; otherwise reserve a standard-height readout below the slider.
- Performance Macro readouts should stay concise inside the cell. Do not repeat the destination/group prefix inline when the group label, control label, preset summary, and tooltip already explain the mapping; cramped macro cells should show the control first and keep detailed register wording in tooltips or the summary line.
- Source-card panels should size from the visible card rows and the controls they actually contain, not stale percentage minima. The card should be tall enough for waveform preview, standard selectors, and a visible level lane, while extra vertical budget goes to sample-bank waveform editors, output scopes, or macro controls instead of empty source-card space.
- Source-card height budgets must include the module title/summary chrome. SPC700 voice cards need enough post-header height for sample selector plus level, and wavetable cards should stay compact by trimming unused body space rather than hiding level lanes.
- Wavetable source decks use a control-derived budget. HuC6280 reserves a 3x2 card matrix plus its LFO relationship; Namco WSG reserves a 4x2 matrix; SCC reserves a 3x2 five-channel matrix plus its topology line. All three reserve the inline Wave Lab and shared footer inside an 880 px editor. Every card must fit header, preview, standard selector, and Level without overlap.
- HuC6280 uses three columns for six role-differentiated voices; Namco WSG uses four for eight equivalent lanes; SCC uses three for five channels plus a full-width enhanced-core topology disclosure. Geometry follows the implemented signal path rather than a family template.
- Extra HuC6280 vertical space belongs to the meaningful channel-pair LFO readout, not blank card bodies or detached generic Envelope/Motion/Output destinations.
- The editor canvas is capped to stay DAW-friendly. Most chip pages launch at the shared 1240 x 860 default. SID uses 880 because per-voice ADSR is central; HuC6280, Namco WSG, and SCC use 880 for inline Wave Lab completeness; SPC700-style and Paula use 900 for voice/sample-bank completeness; DMG, SN76489, YM2149, PC Speaker, and ZX Beeper use 720 when their unified paths fit more clearly without empty vertical padding. Any chip-specific height must be documented and smoke-tested at both supported widths.
- The editor resize limit must advertise the selected chip's documented fixed height. Hosts should not be given an unbounded resize ceiling that can reopen Chipper off-screen.
- The editor timer also re-applies the global width/height clamp after host restore. This is intentional defensive behavior for DAWs that reopen a plugin at a stale oversized window rectangle before the normal resize path settles.
- Editor size is now a smoke-test contract, not just a visual preference. `chipper_editor_size_smoke` checks chip-specific launch heights, verifies that oversized host-restored bounds are clamped back to the DAW-friendly maximum, and walks visible child geometry so dropdowns/buttons do not silently shrink below readable size or drift outside the editor bounds.
- The same editor-size smoke test cycles through every exposed chip mode before forcing an oversized host-restore, so chip-specific layouts cannot quietly reintroduce an off-screen VST after a mode switch. All chip pages must clamp back to their documented fixed height if the host restores a taller window; sample-heavy pages need to protect their waveform/editor controls by rebalancing content or adding a deliberate secondary view, not by accidentally making the plugin taller.
- Chip-specific layout heights should stay explicit. NES, SID, SPC700, Paula, wavetable, and FM/PSG fallback screens have different space budgets; avoid generic sample fallbacks that silently change sampler behavior when one chip needs a special case.
- Paula reserves three simultaneous layers: channel-local DMA sample cards, a protected Sample Bank waveform/editor row, and the two-row shared tracker-playback/output strip. No detached envelope destination remains.
- Sample-focused layouts should not reuse the Sample Bank module as a generic Output panel. Paula and SPC700 route output controls through Performance Macros so the shared file/folder/sample waveform editor keeps a protected rectangle.
- Dense sampler and wavetable cards reserve their level lane in the main top-down control flow after optional shape/sample selectors. This keeps per-channel gain readable for Paula, SPC700, HuC6280, Namco WSG, and SCC even when those cards grow more chip-aware controls. Wavetable cards place the Level lane immediately below the per-lane wave selector instead of bottom-anchoring it inside empty card space. Treat the reserved level lane as the non-negotiable part of the card; if future controls do not fit, grow the card instead of shrinking level controls. Descriptor smoke coverage now also requires every visible source-level spec to keep a Level-named label, help text, APVTS parameter, and MIDI CC mapping so a level lane cannot silently degrade into an unlabeled or unhelpful control. Editor-size smoke also requires sampler Level lanes to keep a readable owned rectangle near their selector, so SPC700 and Paula cannot pass with a token 1-pixel or clipped level control.
- NES / RP2A03 is the DAW-fit guardrail for sample-heavy chip pages: DMC needs a real waveform preview, but the editor should launch compactly enough for normal host windows inside the 860 px cap. Keep the NES channel row, DMC sample row, and Performance Macros bounded together instead of letting the DMC preview or macro strip expand the full plugin off-screen. NES uses a compact stacked Performance Macro layout: first-row macros, a dedicated APU Decay row, then Output/scope. The macro row needs enough height for labels/readouts, while APU Decay and Output must remain separate rows and never collapse into wire-like slivers.
- NES / RP2A03 DMC layout is now covered by editor-size smoke checks: playback mode, sample slot, root note, loop toggle, rate selector, waveform preview, APU Decay, Output, and the output scope must remain readable, owned by their panel, and non-overlapping inside the 1240 x 860 editor cap.
- SPC700 per-voice sample slot metadata is now a descriptor-smoke contract for all eight voices: each voice keeps a standard menu surface in the Sample group, supports preset plus 32 bank slots, explains loaded sample-bank pinning, and remains backed by APVTS plus MIDI CC mapping.
- SPC700 source cards should show the resolved playback lifetime beside the per-voice sample or generated shape label. `Loop` / `1x` in the card header and `loop while held` / `one-shot` in the readout make the existing S-DSP-style sample lifetime visible without pretending loop state is a separate per-voice native register.
- SPC700 and Paula map modes must show an explicit `No mapped sample` state when a MIDI note falls outside the loaded bank span. The status should keep the bank count and mapped key range visible instead of falling back to a fake `Slot 0/0` style label.
- Wavetable-family source decks should stay compact enough to scan, but “compact” is chip-relative. HuC6280 earns its relationship strip; Namco WSG earns an eight-card 4x2 bank; SCC earns a five-card 3x2 bank plus an honest SCC/SCC+ topology line. All three share the 880 px Wave Lab canvas without hiding controls.
- Wavetable cards remain 136-144 px at both supported widths. Direct sample drawing lives in the shared inline Wave Lab rather than expanding every source card; future native balance or routing controls should grow the owning layout only when they cannot fit honestly.
- For HuC6280, the voice deck owns the main vertical budget and `Voice Motion + Output` owns the two-row shared budget. Detached helper-envelope, Motion, and Output modules are retired.
- Sample-family source decks should follow the same control-derived height rule. SPC700 and Paula source cards need enough room for waveform preview, standard sample/shape selectors, and Level lanes, but extra vertical budget should go to the shared sample-bank waveform editor or compact Performance Macros instead of tall empty voice cards.
- SPC700 and Paula sample-bank panels require enough height before switching into the two-column editor layout. Playback, root, loop metadata, and file/folder/bank controls keep standard control height; if the sample bank is too short, the layout must compact or fall back instead of covering the waveform or shared controls. Both sampler pages use a documented 900 px cap.
- Sampler import actions are part of the standard-control contract, not decorative micro-buttons. `chipper_editor_size_smoke` checks SPC700 and Paula File, Folder, and Bank buttons so sample loading remains readable at the default editor size.
- Paula sample-bank layout has priority over macro-strip height. The four channel cards still need readable per-channel shape/sample/level controls, but the shared sample-bank waveform/editor row must stay tall enough to avoid collisions with Performance Macros; shrink the macro strip, cap the waveform preview inside the Sample Bank module, or rebalance channel-card height before shrinking the sample editor.
- Paula sampler layout should balance three visible layers without overlap: per-channel sample cards, the shared sample-bank waveform/editor, and compact Performance Macros. The sample bank is the protected editor; macros are secondary, and channel cards should stay only tall enough for waveform, shape/sample selectors, and level.
- Paula uses a deliberate two-row shared strip because hiding Period Motion, Loop Mode, Output Filter, Stereo Spread, or the Tracker Amp Env is not an acceptable density tradeoff. Future controls must be engine-backed and rebalanced inside the unified surface rather than moved into a destination view.
- SPC700 and Paula sampler layouts now reserve extra source-card height for standard sample dropdowns and visible level lanes, while their Performance Macros strips stay slightly shorter than non-sampler pages. This is intentional: sampler usability depends on seeing channel sample assignment, level, and the shared waveform editor before secondary macro controls.
- Sampler source-card height must be calculated from the full visible control stack, not from a generic row percentage. SPC700 voice cards need room for header, waveform, per-voice sample selector, and level; Paula cards need header, waveform, per-channel generated shape, per-channel sample slot, and level. If these cannot fit, grow the sampler page rather than hiding the dropdown or level lane.
- `chipper_editor_size_smoke` now checks SPC700 and Paula sampler source cards directly: visible source cards must keep readable sample/wave selectors, Paula's second per-channel sample selector, and a visible Level lane owned by the source card.
- Binary sample lifetime controls should read like binary controls. SPC700 loop lifetime is a checkbox-style `Loop While Held` control in the sample bank; one-shot playback is the unchecked state, while detailed playback/register notes stay in the status/readout text.
- Sample-bank panels are primary editors, not status widgets. NES DMC, SPC700, and Paula should show a useful waveform preview, readable file/folder/bank controls, root/playback/loop state, and missing-sample feedback without overlapping Performance Macros. If a helper envelope or macro competes for space with sample import and waveform inspection, move the helper into Performance Macros or grow the layout.
- Sampler and wavetable chips may require different fixed proportions, not an unbounded taller editor. Protect per-channel shape/sample selectors, level sliders, and waveform previews before compressing the page; if the default viewport is not enough, create a chip-specific editor surface or secondary page instead of hiding controls or opening off-screen.
- HuC6280 uses one unified voice deck plus `Voice Motion + Output`. Namco WSG uses one eight-lane bank plus `Lane Motion + Output`. SCC uses one five-channel bank plus `Wave Stack + Output`.
- Performance Macros now show chip-aware destination hints in their resolved readout lines, such as `$400E noise`, `NR43 noise`, `$D415 cutoff`, `AUDV gate`, `Wave RAM`, and `$B0 feedback`. These hints make it clear which macros are musical gestures over native chip paths rather than generic unlabeled sliders.
- PSG, FM, POKEY, and wavetable pages should keep shared controls compact but legible. YM2149/AY, SN76489, OPLL, POKEY, HuC6280, Namco WSG, and SCC must not squeeze long register hints into the same header row as slider labels; compact macro cells reserve separate rows for label, slider, and resolved register readout before any chip-owned source/operator panel is compressed. YM2612, YM2151, and OPL are deliberately different: their complete chip-owned patch controls live above a compact Clock + Output footer.
- YM2612/OPN2 uses a dedicated 860 px unified surface. Six simultaneous source cards make note allocation and Ch 6 ownership visible; Algorithm, Feedback, Operator Tone, and FM Level form one shared patch block; the 2x2 operator matrix states that one carrier/modulator patch is applied to all six FM channels; and Envelope Shape, DAC Mode, LFO Depth, and Pan remain visible together. DAC mode explicitly discloses that current VST sample-file loading is unavailable, while channel 6 changes its card label and preview to the `$2A` DAC stream when active. Algorithm Bias is contextual because it affects audio only for the Manual recipe while Algorithm remains on Preset.
- OPL2/OPL3 uses a dedicated 860 px unified surface rather than the generic FM destination stack. Nine cards form a stable 3x3 lane matrix; `OPL Topology` changes their truthful roles between nine 2-op voices, six melodic voices plus `$BD` percussion, nine paired low/high-bank layers, or three linked `$104` 4-op pairs plus channels 7-9.
- In OPL 4-op mode, cards 1-3 remain key lanes, cards 4-6 become visibly disabled operator-stage followers while retaining their meaningful level trims, and cards 7-9 remain independent 2-op voices. Waveform, `Connection / 4-op Algorithm`, Feedback, Operator Tone, and FM Level stay together in `Topology + Shared Patch`; the adjacent 2x2 `Shared Operator Matrix` edits OP1-OP4 level, multiplier, and native AR/DR/SL/RR fields and labels algorithm-aware carrier/modulator roles; `Active Signal Path` names the selected topology and algorithm; only Clock and Output remain global.
- OPL geometry and topology language are smoke-test contracts at both supported widths. The test exercises Melodic, Rhythm, 18ch Layer, and 4-op Pair choices, all four algorithms, every lane and source level, every operator control/readout, signal-path explanation, and compact footer. `Active Signal Path` now owns the five-way native Stereo Route selector, while each lane header reports its resolved `OUT L`, `OUT R`, `OUT LR`, or low/high-bank route. A dedicated `opl4op` PNG fixture captures algorithm 2 at both widths, and exact renderer fixtures pin both `$C0` connection bits, `$F0/$50/$A0` output-select states, and all four operators' `$20/$40/$60/$80` fields. The compact waveform preview drops its redundant internal label before the trace, and a pixel-level smoke check protects that content-first behavior.
- YM2151/OPM uses its own 860 px unified surface rather than inheriting OPN2 destinations. Eight channels remain visible in a 4x2 bank with native pan and note-allocation state in each header; channel 8 alone owns the five-way `$0F` Noise selector and reports whether operator 4 remains tonal or becomes noise.
- OPM Algorithm, contextual Algorithm Bias, Feedback, Operator Tone, and FM Level form one shared four-operator patch beside the editable 2x2 operator matrix. Each operator card adds one combined DT1/DT2 popup row above a compact resolved register line without shrinking Level/MULT/EG below the standard minimum; exact `$40/$C0` composite bytes stay in the DT tooltip. Native LFO Depth, waveform, PMS, AMS, and the per-channel pan pattern share the final routing block; only Clock and Output remain global. Both widths, direct-choice readability, DT containment/non-overlap, tooltip byte disclosure, channel/noise ownership, explicit-algorithm takeover, Chip Poly labels, routing, browser persistence, and compact-footer geometry are regression contracts.
- Genuine OPM gaps remain engine/product work: independent per-channel operator patches, timer/CSM behavior, exact noise timing, golden comparisons, and hardware validation. The shared current controls—including native per-operator DT1/DT2—must not be duplicated eight times until the engine exposes distinct channel state.
- YM2413/OPLL uses its own 860 px unified surface. Nine cards form a stable 3x3 lane matrix; Instrument and `$0E` topology share one block; and the single programmable User0 patch is shown as shared Modulator and Carrier cards rather than being implied nine times. Explicit ROM instruments visibly disable the preserved User0 editor, while an operator override under Preset/Custom activates User0 across every melodic lane.
- YM2203/OPN uses a dedicated 860 px FM-plus-SSG surface. FM1-3 and SSG A-C form a paired 3x2 bank in Big Mono and become six explicit note lanes in Chip Poly. The FM row owns the one shared four-operator patch and matrix; SSG A/B/C each own their Tone/Noise selector; and the three SSG lanes share one envelope shape/period block. `FM Tone + SSG Noise` and `FM + SSG Level` remain together in the compact footer because the engine intentionally applies those controls across both synthesis sides.
- YM2203 Algorithm Bias follows the same ownership rule as OPN2/OPM: it is enabled only for Manual + Preset Algorithm. Explicit algorithms and non-Manual recipes visibly own the resolved algorithm instead. Feedback remains a native 0-7 menu and must never alter FM pitch relative to the paired SSG lanes; renderer tests pin identical F-number/block state at feedback 0 and 7.
- YM2608/OPNA uses a dedicated 900 px surface rather than the generic four-operator destination stack. Six FM lanes occupy the first two rows and SSG A-C the third; every SSG card owns its Tone/Noise selector. In Drum/Hit the FM cards explicitly add BD, SD, Top Cym, HH, Tom, and Rim because those same six enable/level controls gate and scale the native ADPCM-A rhythm instruments. ADPCM-B remains one optional shared layer, not a tenth note lane.
- OPNA's shared FM patch and operator matrix remain distinct from its shared SSG envelope generator and conditional rhythm/sample memory. The compact footer contains only the two intentional FM/SSG bridge controls, native pan, modern stereo width, clock, and output. Algorithm Bias is contextual, SSG period is surfaced through the existing stable envelope parameter, and Feedback 0-7 is regression-tested as pitch-neutral at the same F-number/block.
- YM2610/OPNB uses a dedicated 900 px surface rather than inheriting the generic OPNB destination stack. FM1-4 form the first row and SSG A-C an intentional centered second row; each SSG card owns its Tone/Noise mix. Drum/Hit relabels the first six lane controls with ADPCM-A1-6 because the engine uses FM1-4 and SSG A-B enable/level state to gate and scale those external-memory segments. SSG C correctly remains unpaired.
- OPNB's shared FM patch/operator matrix and shared SSG generator remain separate from external encoded ADPCM-A/B memory. The empty state says that both memories are silent until loaded and that they are Drum/Hit-only layers. The footer contains only FM/SSG tone and level bridges, native pan, modern width, clock, and output. SSG period is surfaced through the stable envelope parameter, and Feedback 0-7 is regression-tested as pitch-neutral for both YM2610 and its shared YM2610B core.
- YM2610B/OPNB2 uses its own 900 px surface rather than stretching the four-FM OPNB arrangement. FM1-6 and SSG A-C form a stable 3x3 allocation bank; every SSG card owns its Tone/Noise mix, and Big Mono or Chip Poly allocation remains legible in every lane header. Drum/Hit adds ADPCM-A1-6 ownership only to FM1-6 because those six enable/level controls gate and scale the six external-memory segments; SSG A-C never pretend to own sample segments.
- OPNB2 keeps one shared four-operator FM patch and matrix, one shared SSG envelope shape/period and noise generator, and one conditional external-memory block. Its empty state identifies both ADPCM memories as silent Drum/Hit-only layers, with ADPCM-A segmented across FM1-6 and ADPCM-B remaining one shared pitched layer. The compact footer carries only FM/SSG tone and level bridges, native pan, modern width, the 8.00 MHz clock, and output. The stable CC88 parameter now exposes SSG envelope period, and the shared feedback-pitch regression gate covers the six-FM mask.
- OPLL rhythm ownership is a behavioral contract. Cards 7-9 become BD (`$36`), HH+SD (`$37`), and TOM+CYM (`$38`). Lanes 1-6 remain active for an explicit Rhythm layer in Big Mono with a non-drum recipe, become idle for Drum/Hit, and also become idle when Chip Poly routes every incoming note to the rhythm set. The UI disables only truly inaudible lanes and never clears their saved enable/level values.
- The stable `ym2413.pitchSpread` automation ID remains unchanged, but its visible label is `Tuning Offset`: the engine applies one -6..+6 semitone offset to every melodic lane, not inter-lane spread. Instrument Bias is active only for Manual + Preset/Custom before User0 takes over; Motion is active only for the Laser recipe or Manual User0 Follow fields. Both widths, ROM/User0 takeover, melodic/rhythm allocation, native footer text, browser persistence, and operator containment are regression contracts.
- Compact PSG/FM macro readouts should favor register-native shorthand in visible cells (`Reg 7`, `Reg E`, `st`, `tone+noise`) while preserving fuller explanations in tooltips, source-card detail, or preset summaries.
- YM2149/AY Tone/Noise Mix is a segmented hardware mixer choice, not a slider. Its Performance Macro slot must reserve a dedicated label, segmented choice row, and resolved register readout so Noise/Tone/Both remains readable at the fixed 1240 x 860 editor size.
- Sampler pages keep Performance Macros secondary to source cards and sample-bank editors, but visible macro sliders still need readable rails. SPC700 and Paula use a single compact row for the currently exposed macro controls so their sliders remain usable without reclaiming height from sample assignment, waveform preview, or file/folder/bank controls.
- The preset browser now groups factory sounds by chip-local musical category, shows counts in the section headers, and includes compact metadata plus search filtering in the VST header. The metadata filter keeps Role, Engine, and Tag choices in one dropdown so Furnace-informed tags such as `noise`, `fm`, `sampler`, `wavetable`, and `tracker` are usable without adding another cramped header control. User presets remain a separate flat-file bank, but saved files carry role/engine/tag metadata when written by Chipper, and metadata-bearing user presets participate in the same filter/search path. A chip-local Init Patch entry sits above both banks for a neutral reset point that preserves the current Strictness and supported Play Mode. This keeps browsing useful without changing the simple `.chipperpreset` sharing model.
- Descriptor smoke tests now lock the public chip catalog to named hardware families only, assert that `Arcade Hybrid` and `Custom` do not return as undefined selector modes, and require each exposed chip to have factory presets available in the UI.
- Descriptor smoke tests also act as the first UI contract for chip-owned controls: NES/DMG pulse duty and noise choices, SPC700/Paula sample slots, HuC6280/Namco/SCC wave selectors, source enable toggles, and source level trims must remain tied to source-card surfaces or explicitly documented shared modules.
- Shared APVTS/MIDI slots should use generic host-facing names when their chip meaning changes by mode. CC94 remains the stable parameter behind DMG Stereo Route, SID Model, SPC700/Paula loop behavior, POKEY AUDCTL pairing, HuC6280 LFO mode, FM pan/routing, and some final wavetable lane choices, so the host/default map calls it `Chip Choice / Route` while each chip panel shows the descriptor-specific label.
- Active chip panels should describe what the current build can do, while verification gaps and future work belong in the footer tooltip, accuracy docs, and roadmaps. Avoid dead-looking rows such as `Reference tests needed`, `Custom patch planned`, or `Drive planned` inside playable chip modules; use current-state labels such as `Verified partial`, `Operator EG readout`, `Model color`, or the exact register path instead.
- Retired or unknown chip descriptors are migration fallbacks only. If an old session somehow reaches that surface, it should say `Unavailable` / `Migration only` and point the user toward named hardware-family modes rather than presenting planned modules or half-productized generic engines.

## Highest-Value Next Fixes

1. Preset browser and preset sharing
   - Current UI: the global browser is the dedicated preset overlay and now includes grouped chip navigation, role/text filtering, favorites, recents, recursive user-bank discovery, detail copy, chip-local Init Patch entries, and explicit cross-chip loading. A Furnace-inspired coverage matrix is exported and enforced without copying preset data.
   - Genuine gap: deeper user-bank management, bulk metadata editing, preset rename/delete workflows, and a systematic audibility/loudness/visible-recall QA pass remain. Factory presets must stay original Chipper parameter snapshots with clean provenance, and user presets should remain easy to share.
   - User value: very high. Most musicians will browse for "arcade bass" or "Game Boy lead" before they know which chip engine they want.
   - Confidence: 7/10. Preset data already exists; browser UX and save/load polish are the main work. Confidence improves with a preset QA pass that checks audibility, loudness, visible-control recall, and clean provenance for every factory preset.

2. Unified chip surfaces
   - Implemented direction: Play / Edit / Inspect workspace navigation has been removed, and every one of the 27 implemented modes now has a reviewed unified surface. Each surface exposes every meaningful lane and keeps native controls with the owning channel or shared generator; the global preset browser and Motion Lab are the only separate overlays.
   - Next value: deepen only engine-backed controls and editors; do not create another layout destination to represent behavior the engine does not expose.
   - Non-regression rule: never reintroduce workspace switching as a way to hide ordinary sound-design controls. Unified layouts must preserve sound, automation, MIDI state, preset state, chip-local snapshots, source ownership, accessibility, and both supported editor widths.

3. Motion Lab
   - Delivered workflow: one global overlay edits an independent eight-step pattern for every canonical chip with pitch, explicitly post-chip level, Hold/Trig/Cut, 1-8 length, 1/8-1/64 rates, six templates, chip-aware destination/status text, and active-step text plus color. It is sample-accurate in Big Mono, visibly bypassed in Chip Poly, and recalled through current schema-v6 non-parameter state.
   - Non-regression rule: preserve the 1180/1240 eight-column layout, standard control sizes, explicit focus order/names, lock-free publication, no audio-thread allocation, active-step non-color cue, per-chip isolation, and deterministic browser/overlay replacement.
   - Genuine gap: future native duty, wave, noise, vibrato, and one-shot SFX destinations need real engine/register state and tests; do not add decorative destinations or imply that the current 0-15 post-chip level is a native register write.
   - Confidence: 9/10 for the delivered foundation. Chip Poly motion remains intentionally out of scope until a deterministic per-voice policy exists.

4. SPC700 fidelity and independent voice state
   - Current UI: the unified 900 px surface now exposes all eight voice/sample paths, generated fallback source, shared shaping/PMON/noise behavior, bank mapping, loop range, echo/mix, and output without tabs or hidden destinations.
   - Genuine gap: the partial core still needs independent per-voice ADSR/GAIN, NON, PMON, pitch/loop state, exact BRR directory/loop semantics, Gaussian interpolation, echo/FIR behavior, timing, and hardware/reference validation. Do not manufacture eight copies of shared parameters in the UI before those states exist in the engine.
   - User value: medium-high. The instrument is already understandable and playable; the next gain is deeper authentic control rather than another layout pass.
   - Confidence: 5/10. The remaining work crosses engine architecture, parameter compatibility, presets, MIDI allocation, and validation evidence.

5. Wavetable voice polish
   - Delivered workflow: HuC6280, Namco WSG, and SCC now share an inline native 32-sample Wave Lab with live drawing, keyboard editing, cross-depth copy/paste, WAV/AIFF import, generated-wave reset, and exact project recall through versioned non-parameter state.
   - Genuine gap: HuC6280 still needs native balance and stronger DDA/noise/LFO validation. Namco needs genuine chip-variant/voice-count modeling, waveform-addressing and DAC/output validation, and audited routing. SCC needs a real SCC/SCC+ distinction, original channel 4/5 shared-wave behavior where applicable, mapper/bank behavior, DAC/output validation, and reference evidence.
   - Confidence: 8/10. The reusable editor and state path are release-gated; the remaining work is chip-model authenticity rather than generic per-lane editing.

6. FM operator editors
   - Current UI: OPN2, OPM, OPN, OPNA, OPNB, OPNB2, and OPL3 expose carrier/modulator roles, a four-operator matrix, native feedback, and the currently implemented per-operator level/multiplier/envelope fields. OPN/OPM use algorithm graphs; OPL uses its topology and two linked connection bits; OPLL uses the topology-appropriate shared two-operator User0 editor.
   - Genuine gap: independent per-channel patches, deeper native detune/LFO/sensitivity controls, OPL tremolo/vibrato/KSR/KSL and independently addressable output-bus routing, dedicated OPL3 18-channel editing, OPLL patch-set variants, and stronger golden/hardware validation remain engine and product work.
   - User value: very high for FM users.
   - Confidence: 5/10. The remaining work changes engine state, automation, preset compatibility, and validation rather than only adding UI.

7. Paula tracker import depth
   - Current UI: the unified four-channel DMA surface, explicit shared loop/period/volume/gate/filter controls, sample-bank mapping, waveform evidence, imported uncompressed 8SVX, WAV `smpl`, AIFF `MARK`/`INST`, and first-pass ProTracker MOD sample loop metadata are in place.
   - Genuine gap: full MOD playback/effects, tracker retrigger commands, 14-bit/channel-pair options, independent per-channel period/volume/loop parameters, compressed 8SVX, DMA/CIA timing, and analog-output validation remain engine work. Do not imply these through decorative UI controls.
   - User value: medium-high. The current surface already behaves like a four-channel tracker sampler; the next value is deeper module behavior and authenticity.
   - Confidence: 5/10. The remaining work crosses file parsing, playback scheduling, parameter compatibility, and validation evidence.

8. Behavior strictness and register text presentation
   - Issue: exact register readouts help prove honesty, but they compete with musical labels in the default view.
   - User value: high. Musicians should see what they hear first, while advanced users can still inspect register-level behavior.
   - Confidence: 7/10. Requires a consistent Expert/detail overlay and tooltip policy across chips.

9. All-chip screenshot audit
   - Current release gate: the fresh 164-image matrix covers all 27 implemented modes in editor, preset browser, and Motion Lab at 1180 and 1240 px, plus the dedicated OPL3 four-operator workspace at both widths. The capture wrapper accepts `motion` and `opl4op` directly. Structural tests cover source ownership, standard control sizes, compact heights, preview content, overlay persistence/replacement, focus contracts, Motion Lab active-state semantics, and obsolete workspace removal.
   - Genuine gap: host-specific DPI/scaling comparison and stable pixel-diff baselines across supported operating systems remain; the current renderer is the authoritative geometry/visual smoke gate.
   - User value: high. Users should never need to guess whether a control is hidden, clipped, or decorative.
   - Confidence: 8/10. Repeatable captures and structural assertions catch the known layout regressions before release.

10. Expand audio non-regression smoke checks
   - Issue: FM held-tail assertions and NES DMC loop-off assertions are now release gates, not open UI bugs. The next coverage value is extending that same confidence to sample, loop, helper-envelope, and source-card changes that can look correct while regressing key-on/key-off, one-shot/loop behavior, or sustained output.
   - User value: high. A playable instrument must hold notes predictably before deeper editor polish matters.
   - Confidence: 8/10. Existing renderer and processor smoke tests already expose source levels, key-on state, sample loop state, `tailRms`, and debug JSON; the next value is expanding the same assertion style beyond FM.

## Design Rule

When a control maps to a channel-local register, place it in or directly under that channel's card. Reserve shared panels for genuinely shared hardware: filters, output routing, sample banks, global clocks, echo, LFOs, and cross-channel pairing.

When a chip needs more detail than the current card can hold, grow the chip layout or add a chip-specific editor surface. Do not hide the control, shrink it below the standard control height, or move it into a vague global macro area just to preserve the old grid.

## Cleanup Rule

After a screenshot-driven fix lands, keep the durable rule and remove the one-off complaint from active planning language. The audit should answer "what class of UI regression are we preventing now?" rather than preserve a chronological bug diary.
